<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | F1 Archívum</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/">F1 Archívum</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div id="nav" class="collapse navbar-collapse">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item"><a class="nav-link <%= active==='home'?'active':''%>" href="/">Főoldal</a></li>
        <li class="nav-item"><a class="nav-link <%= active==='db'?'active':''%>" href="/adatbazis">Adatbázis</a></li>
        <% if (user && user.role === 'admin') { %>
          <li class="nav-item"><a class="nav-link <%= active==='pilota'?'active':''%>" href="/pilota">CRUD (Pilóta)</a></li>
        <% } %>
        <li class="nav-item"><a class="nav-link <%= active==='messages'?'active':''%>" href="/uzenetek/kapcsolat">Kapcsolat</a></li>

        <% if (user && (user.role === 'registered' || user.role === 'admin')) { %>
          <li class="nav-item"><a class="nav-link <%= active==='uzenetek'?'active':''%>" href="/uzenetek">Üzenetek</a></li>
        <% } %>

        <% if (user && user.role === 'admin') { %>
          <li class="nav-item"><a class="nav-link <%= active==='admin'?'active':''%>" href="/admin">Admin</a></li>
        <% } %>
      </ul>

      <ul class="navbar-nav ms-auto">
        <% if (!user) { %>
          <li class="nav-item"><a class="nav-link" href="/login">Bejelentkezés</a></li>
          <li class="nav-item"><a class="nav-link" href="/register">Regisztráció</a></li>
        <% } else { %>
          <li class="nav-item">
            <span class="navbar-text me-2"><%= user.email %> (<%= user.role %>)</span>
          </li>
          <li class="nav-item">
            <form method="post" action="/logout">
              <button class="btn btn-outline-light btn-sm">Kijelentkezés</button>
            </form>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<div class="container mt-4">
  <% if (success && success.length) { %>
    <div class="alert alert-success"><%= success %></div>
  <% } %>
  <% if (error && error.length) { %>
    <div class="alert alert-danger"><%= error %></div>
  <% } %>
  <%- body %>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
